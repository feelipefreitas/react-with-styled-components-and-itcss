import { createGlobalStyle } from 'styled-components';

const GlobalColors = createGlobalStyle`
    :root {
        --buttom-backcolor: #fff;
        --buttom-color: #3b86ff;

        --card-textcolor: #fff;
        --card-backcolor: #3b86ff;
    }
`;

export default GlobalColors;